import { ObjetToarrayPipe } from './objet-toarray.pipe';

describe('ObjetToarrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjetToarrayPipe();
    expect(pipe).toBeTruthy();
  });
});
